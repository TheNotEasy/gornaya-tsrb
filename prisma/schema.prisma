datasource db {
    provider = "postgresql"
}

generator client {
    provider = "prisma-client"
    output = "../generated"
}

model GalleryImage {
    id Int @id @default(autoincrement())
    url String @unique
    caption String
}